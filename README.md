# Projeto Rápido e Seguro

Esta é a documentação da API do projeto Rápido e Seguro.

## Como rodar o projeto
1. Instale as dependências: `npm install`
2. Rode o servidor: `nodemon .\server.js`
3. Gere a documentação: `npm run doc`